{
  "name": "json-schemas",
  "private": true,
  "scripts": {
    "test": "bun run scripts/build.js && bun scripts/test.js"
  },
  "devDependencies": {
    "ajv": "^8.20.0",
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.1",
    "glob": "^13.0.0",
    "prettier": "^3.7.4",
    "smol-toml": "^1.6.0",
    "yaml": "^2.8.2"
  }
}
